# Supabase Configuration
# Get these from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Google Gemini API (Nano Banana - Phase 3 Image Generation)
# Get API key from: https://aistudio.google.com/apikey
GOOGLE_GEMINI_API_KEY=your-gemini-api-key

# Backend Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Frontend Configuration (copy to frontend/.env.local)
# VITE_SUPABASE_URL=https://your-project-id.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
# VITE_API_URL=http://localhost:3001

# =============================================================================
# AI Tools Mock Configuration
# =============================================================================
# Toggle between real API calls and mock responses.
# This enables frontend development without API costs and predictable testing.

# Master toggle - controls how individual toggles are interpreted:
#   API        = Force ALL tools to use real API (ignore individual toggles)
#   MOCK       = Force ALL tools to use mocks (ignore individual toggles)
#   PER_TOGGLE = Respect individual toggle settings below
MOCK_ALL_AI_TOOLS=API

# Individual tool category toggles (only used when MOCK_ALL_AI_TOOLS=PER_TOGGLE)
#   API  = Use real API calls
#   MOCK = Use mock responses
MOCK_AI_SERVICE=API
MOCK_RESEARCH_TOOLS=API
MOCK_SKELETON_TOOLS=API
MOCK_CONTENT_WRITING_TOOLS=API
MOCK_HUMANITY_CHECK_TOOLS=API
MOCK_TOPICS_RESEARCH_TOOLS=API
MOCK_VISUALS_CREATOR_TOOLS=API
MOCK_IMAGE_GENERATION_TOOLS=API

# Mock behavior configuration
MOCK_DELAY_MIN_MS=500        # Minimum simulated delay (ms)
MOCK_DELAY_MAX_MS=2000       # Maximum simulated delay (ms)

# Response capture mode - when enabled, saves real API responses to disk
# for generating mock data files. Useful for:
#   1. Creating initial mock data from real responses
#   2. Updating mocks when API output format changes
#   3. Debugging differences between mock and real responses
# Files are saved to: ./logs/captured-responses/{toolName}/{variant}/
MOCK_CAPTURE_RESPONSES=false
