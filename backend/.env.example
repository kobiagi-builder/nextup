# Supabase Configuration
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=xxx
SUPABASE_SERVICE_ROLE_KEY=xxx

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# AI Services
# Tavily API for web search (https://tavily.com)
TAVILY_API_KEY=xxx

# Google Gemini API for content writing (https://ai.google.dev/)
GOOGLE_GENERATIVE_AI_API_KEY=xxx

# OpenAI API for DALL-E 3 image generation (https://platform.openai.com)
OPENAI_API_KEY=xxx

# Image Generation Configuration
# Options: dalle | gemini (default: dalle)
# - dalle: Uses OpenAI DALL-E 3 (~$0.04/image standard, ~$0.08/image HD)
# - gemini: Uses Google Imagen 4 (requires billing enabled on Google Cloud)
IMAGE_GENERATION_SERVICE=dalle

# Mock Configuration (optional)
# Master toggle: API (all real) | MOCK (all mocked) | PER_TOGGLE (use individual settings)
MOCK_ALL_AI_TOOLS=API

# Individual service toggles (only used when MOCK_ALL_AI_TOOLS=PER_TOGGLE)
# Options: API (use real API) | MOCK (use mock data)
MOCK_AI_SERVICE=API                    # Claude main agent
MOCK_RESEARCH_TOOLS=API                 # Tavily web search
MOCK_SKELETON_TOOLS=API                 # Claude skeleton generation
MOCK_CONTENT_WRITING_TOOLS=API          # Gemini content writing (set to MOCK to avoid quota limits)
MOCK_HUMANITY_CHECK_TOOLS=API           # Claude humanity checking
MOCK_TOPICS_RESEARCH_TOOLS=API          # Claude topics research

# Mock behavior settings
MOCK_DELAY_MIN_MS=500                   # Minimum simulated delay (ms)
MOCK_DELAY_MAX_MS=2000                  # Maximum simulated delay (ms)
MOCK_CAPTURE_RESPONSES=false            # Capture real API responses for mock generation
MOCK_CAPTURE_DIR=./logs/captured-responses  # Directory for captured responses

# Quick Setup Examples:
# 1. Avoid Gemini quota limits (recommended):
#    MOCK_ALL_AI_TOOLS=PER_TOGGLE
#    MOCK_CONTENT_WRITING_TOOLS=MOCK
#
# 2. Mock everything (no API keys needed):
#    MOCK_ALL_AI_TOOLS=MOCK
#
# 3. Production (all real APIs):
#    MOCK_ALL_AI_TOOLS=API
